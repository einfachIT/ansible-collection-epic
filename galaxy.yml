### REQUIRED
namespace: einfachit

name: epicserver

#version is replaced on release createion on github
version: 1.0.0

readme: README.md

authors:
- bastian bukatz <bastian.greuel@gmail.com>


### OPTIONAL but strongly recommended
# A short summary description of the collection
description: collection to configure server environment of the einfachit programming lessons

# Either a single license or a list of licenses for content inside of a collection. Ansible Galaxy currently only
# accepts L(SPDX,https://spdx.org/licenses/) licenses. This key is mutually exclusive with 'license_file'
license:
- GPL-2.0-or-later

# requirements as 'namespace' and 'name'
tags: []

dependencies: {}

repository: https://github.com/einfachIT/ansible-collection-epicserver.git

documentation: https://github.com/einfachIT/ansible-collection-epicserver/wiki

homepage: https://einfach-it.ch

issues: https://github.com/einfachIT/ansible-collection-epicserver/issues

# A list of file glob-like patterns used to filter any files or directories that should not be included in the build
# artifact. A pattern is matched from the relative path of the file or directory of the collection directory. This
# uses 'fnmatch' to match the files or directories. Some directories and files like 'galaxy.yml', '*.pyc', '*.retry',
# and '.git' are always filtered
build_ignore:
- '*.tar.gz'
- venv
