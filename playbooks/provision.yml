---
- hosts: containers
  gather_facts: false
  connection: local
  tasks:
    - name: Start a container
      docker_container:
        name: "{{ inventory_hostname }}"
        image: "{{ image }}"
        command: ["sleep", "infinity"]
      delegate_to: 127.0.0.1
